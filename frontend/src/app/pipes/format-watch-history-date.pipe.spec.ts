import { FormatWatchHistoryDatePipe } from './format-watch-history-date.pipe';

describe('FormatWatchHistoryDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWatchHistoryDatePipe();
    expect(pipe).toBeTruthy();
  });
});
